export const selectPages = (state) => state.pages;
export const selectSelectedTopic = (state) => state.selectedTopic;
export const selectContactPage = (state) => state.selectedContactPage;
export const selectLoadingPages = (state) => state.pages.isLoading;
export const statusCodeCMS = (state) => state.errors;
