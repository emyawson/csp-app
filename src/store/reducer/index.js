import pages from './pages';
import selectedTopic from './selectedTopic';
import selectedContactPage from './selectedContactPage';

// Export  reducer  modules
const reducers = {
  pages,
  selectedTopic,
  selectedContactPage,
};
export default reducers;
